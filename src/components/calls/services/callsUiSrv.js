(function (angular) {
    'use strict';

    angular.module('znk.infra.calls').service('CallsUiSrv',
        function () {
            'ngInject';
            // @todo(oded) will implement here the ui (popups)
        }
    );
})(angular);
